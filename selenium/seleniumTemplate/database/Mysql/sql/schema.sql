DROP TABLE IF EXISTS Traveltest;
CREATE TABLE spotDTO(
    id INT NOT NULL AUTO_INCREMENT PRIMARY KEY,
    name VARCHAR(255),
    address VARCHAR(255),
    create_at VARCHAR(255),
    update_at VARCHAR(255),
    __version__ VARCHAR(255)
);